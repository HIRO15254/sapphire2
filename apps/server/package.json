{
  "name": "server",
  "type": "module",
  "main": "src/worker.ts",
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "check-types": "tsc -b",
    "dev": "kill-port 8787 && wrangler dev"
  },
  "dependencies": {
    "@hono/trpc-server": "^0.4.2",
    "@sapphire2/api": "workspace:*",
    "@sapphire2/auth": "workspace:*",
    "@sapphire2/db": "workspace:*",
    "@trpc/server": "catalog:",
    "better-auth": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260405.1",
    "@sapphire2/config": "workspace:*",
    "@types/bun": "catalog:",
    "kill-port": "^2.0.1",
    "typescript": "catalog:",
    "vitest": "^4.1.2",
    "wrangler": "^4.80.0"
  }
}
