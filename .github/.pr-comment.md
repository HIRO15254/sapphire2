## Implementation Complete

**Tests**: All pass
**Changed files**: 10

<details>
<summary>Show implementation summary</summary>

### Changed files

- `packages/db/src/constants.ts` (new): GAME_VARIANTS and DEFAULT_TRANSACTION_TYPES constants
- `packages/db/src/schema/store.ts` (new): Store, Currency, TransactionType, CurrencyTransaction schemas with relations
- `packages/db/src/schema/ring-game.ts` (new): RingGame schema with relations
- `packages/db/src/schema/tournament.ts` (new): Tournament and BlindLevel schemas with relations
- `packages/db/src/schema.ts` (updated): Added all new schemas and relations
- `packages/db/src/migrations/0001_add_store_currency_game_schemas.sql` (new): Migration SQL
- `packages/db/src/migrations/meta/_journal.json` (updated): Added migration entry
- `packages/db/src/migrations/meta/0001_snapshot.json` (new): Migration snapshot
- `apps/web/src/components/mobile-nav.tsx` (updated): Added Stores navigation item
- `apps/web/src/__tests__/mobile-nav.test.tsx` (updated): Updated test for 5 nav items

### Test results

4 tests passed (mobile-nav), type check passed on all packages

### Implementation overview

- **T009**: Created `packages/db/src/constants.ts` with GAME_VARIANTS (NL Hold'em with SB/BB/Straddle blind labels) and DEFAULT_TRANSACTION_TYPES
- **T010**: Created Store, Currency, TransactionType, CurrencyTransaction schemas in `packages/db/src/schema/store.ts` with full FK relations and cascade rules
- **T011**: Created RingGame schema in `packages/db/src/schema/ring-game.ts` with nullable currency FK (set null on delete)
- **T012**: Created Tournament and BlindLevel schemas in `packages/db/src/schema/tournament.ts`
- **T013**: Updated `packages/db/src/schema.ts` to export all new tables and relations
- **T014**: Manually authored migration `0001_add_store_currency_game_schemas.sql` (drizzle-kit generate was blocked by interactive prompt due to todo table removal in Phase 1)
- **T015**: Local D1 migration apply skipped (requires wrangler local environment)
- **T016**: Added Stores nav item (Building2 icon, /stores) to mobile-nav.tsx; sidebar-nav.tsx uses NAVIGATION_ITEMS from mobile-nav so inherits the change automatically

</details>

---
Please review. Approve to continue to Phase 3 (User Story 1 - Store Management) | Request Changes to revise.
