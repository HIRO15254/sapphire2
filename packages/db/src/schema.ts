import {
	account,
	accountRelations,
	session,
	sessionRelations,
	user,
	userRelations,
	verification,
} from "./schema/auth";
import { todo } from "./schema/todo";

export const schema = {
	account,
	accountRelations,
	session,
	sessionRelations,
	todo,
	user,
	userRelations,
	verification,
};
